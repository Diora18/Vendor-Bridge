# VendorBridge API Routes

The backend exposes REST routes under `/api`.

Use `Authorization: Bearer <token>` for protected routes.

## Main Groups

```txt
/api/auth
/api/dashboard
/api/users
/api/vendors
/api/rfqs
/api/quotations
/api/approvals
/api/purchase-orders
/api/invoices
/api/notifications
/api/activity-logs
/api/reports
```

See `MERN_PROJECT_STRUCTURE.md` for the detailed endpoint list.

